export interface Badge {
  badgeId: string;
  badgeName: string;
  badgeDesp: string;
  badgeImage: string;
  badgeAchievedDate: string;
}

export interface UnAchievedBadge {
  badgeId: string;
  badgeName: string;
  badgeDesp: string;
  badgeImage: string;
}
